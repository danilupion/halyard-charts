# Default values for maintainerr
# Automated media cleanup for Plex ecosystem

# Override names
nameOverride: ""
fullnameOverride: ""

# Image configuration
image:
  repository: ghcr.io/maintainerr/maintainerr
  tag: "2.26.0@sha256:91b0770ad7f21d1bdc6dcf27955f2264e34498345ba1e790141ed703301c6918"
  pullPolicy: IfNotPresent

# Security context - maintainerr uses runAsUser instead of PUID/PGID
securityContext:
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

# Timezone
timezone: "UTC"

# Maintainerr-specific configuration
maintainerr:
  port: 6246

# Service configuration
service:
  type: ClusterIP
  port: 6246

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Data persistence
persistence:
  data:
    enabled: true
    existingClaim: ""
    mountPath: /opt/data

# Resource limits
resources: {}

# Deployment strategy - Recreate for single-replica stateful apps
strategy:
  type: Recreate

# Probes
livenessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 5
  httpGet:
    path: /api/health
    port: http

readinessProbe:
  enabled: true
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 3
  httpGet:
    path: /api/health
    port: http
