# Default values for sonarr
# Instance-agnostic chart - use fullnameOverride for multi-instance deployments

# Override names for multi-instance deployments
nameOverride: ""
fullnameOverride: ""  # e.g., "sonarr-es" for Spanish instance

# Image configuration
image:
  repository: lscr.io/linuxserver/sonarr
  tag: "4.0.17@sha256:76414c033f290d3c9f1f9dfad71150abe71d92592369a3377a5903d579e6e2b2"
  pullPolicy: IfNotPresent

# LinuxServer container settings
linuxserver:
  puid: 1000
  pgid: 1000
  tz: "UTC"

# Sonarr-specific configuration
sonarr:
  port: 8989

# Service configuration
service:
  type: ClusterIP
  port: 8989

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Config persistence (per-instance)
persistence:
  config:
    enabled: true
    existingClaim: ""
    mountPath: /config

# Storage mounts - flexible list of volumes
# Each entry can use either hostPath or existingClaim
#
# Example configurations:
#
# Single mount with subPath (hardlinks work):
#   storage:
#     - name: data
#       mountPath: /data
#       existingClaim: disk2-storage
#       subPath: series
#
# Multiple mounts:
#   storage:
#     - name: downloads
#       mountPath: /downloads
#       existingClaim: downloads-pvc
#     - name: series
#       mountPath: /series
#       hostPath: /mnt/series
#
# For hardlinks to work, downloads and media must be on the same mount point
storage: []
#  - name: data
#    mountPath: /data
#    # Use ONE of: existingClaim or hostPath
#    existingClaim: ""
#    hostPath: ""
#    # Optional: subPath within the volume
#    subPath: ""

# Resource limits
resources: {}

# Deployment strategy - Recreate for single-replica stateful apps
strategy:
  type: Recreate

# Probes
livenessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 5
  tcpSocket:
    port: http

readinessProbe:
  enabled: true
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 3
  tcpSocket:
    port: http
