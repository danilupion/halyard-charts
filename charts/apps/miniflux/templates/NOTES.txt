Miniflux has been deployed!

{{- if .Values.route.enabled }}
Access Miniflux at:
{{- range .Values.route.hostnames }}
  https://{{ . }}
{{- end }}
{{- else }}
Get the application URL by running:
  kubectl port-forward svc/{{ include "common.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Then visit: http://localhost:{{ .Values.service.port }}
{{- end }}

NOTE: Ensure PostgreSQL is running and the miniflux database exists before deploying.
