# Default values for home-assistant
# Open source home automation

# Override names
nameOverride: ""
fullnameOverride: ""

# Image configuration
image:
  repository: ghcr.io/home-assistant/home-assistant
  tag: "stable@sha256:0e091dfce3068339c3e1d14382e6c34141e05cd589a1972ebd4d9a8e6b5d8969"
  pullPolicy: IfNotPresent

# Timezone
timezone: "UTC"

# Home Assistant-specific configuration
homeAssistant:
  port: 8123

# Service configuration
service:
  type: ClusterIP
  port: 8123

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Config persistence
persistence:
  config:
    enabled: true
    existingClaim: ""
    mountPath: /config

# Resource limits
resources: {}

# Deployment strategy
strategy:
  type: Recreate

# Host network mode (required for device discovery)
hostNetwork: false

# Privileged mode (required for USB devices like Zigbee)
privileged: false

# Probes
livenessProbe:
  enabled: true
  initialDelaySeconds: 60
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 5
  httpGet:
    path: /
    port: http

readinessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 3
  httpGet:
    path: /
    port: http
