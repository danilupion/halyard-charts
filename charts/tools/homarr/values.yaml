# Default values for homarr
# Modern homelab dashboard

# Override names
nameOverride: ""
fullnameOverride: ""

# Image configuration
image:
  repository: ghcr.io/homarr-labs/homarr
  tag: "latest@sha256:fee5fc51ec4a4bc9b41dcbda88b3b7496117264b791e2ad8e8346d7d623996b4"
  pullPolicy: IfNotPresent

# Timezone
timezone: "UTC"

# Homarr-specific configuration
homarr:
  port: 7575

# Service configuration
service:
  type: ClusterIP
  port: 7575

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Config persistence
persistence:
  appdata:
    enabled: true
    existingClaim: ""
    mountPath: /appdata

# Resource limits
resources: {}

# Deployment strategy
strategy:
  type: Recreate

# Probes
livenessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 5
  httpGet:
    path: /
    port: http

readinessProbe:
  enabled: true
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 3
  httpGet:
    path: /
    port: http

# Secret for encryption key
# Must be 64 character hex string (generate with: openssl rand -hex 32)
secretEncryptionKey:
  existingSecret: ""
  secretKey: "SECRET_ENCRYPTION_KEY"

# HTTPRoute configuration (Gateway API) - the successor to ingress for the
# Envoy Gateway tiers. ingress: and route: are independent; both may be
# enabled during a migration window.
route:
  enabled: false
  # Gateway(s) to attach to, e.g.:
  #   - name: gateway-private
  #     namespace: gateway-private
  #     sectionName: https
  parentRefs: []
  hostnames: []
  # Optional full override of spec.rules; default is a single "/" PathPrefix
  # rule to the chart service
  rules: []
  # Optional HTTPRoute annotations (e.g. dns/managed: "false")
  annotations: {}
  # Optional rule timeouts, e.g. request: 3600s (long-lived connections)
  timeouts: {}
