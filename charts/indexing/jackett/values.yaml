# Default values for jackett

# Image configuration
image:
  repository: linuxserver/jackett
  tag: "0.24.2389@sha256:815c6f8847c9b2f02c3e3b02ef83c5700c3a991b7dee1f505c76fa18749d3991"
  pullPolicy: IfNotPresent

# LinuxServer container settings
linuxserver:
  puid: 1000
  pgid: 1000
  tz: "UTC"

# Jackett-specific configuration
jackett:
  port: 9117
  # Auto-update jackett on container restart
  autoUpdate: false

# Service configuration
service:
  type: ClusterIP
  port: 9117

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Persistence for config
persistence:
  config:
    enabled: true
    # type: hostPath or pvc
    type: pvc
    # For hostPath type
    hostPath: ""
    # For pvc type
    existingClaim: ""
    storageClassName: ""
    accessModes:
      - ReadWriteOnce
    size: 1Gi
    # Mount path inside container
    mountPath: /config

# Resource limits
resources: {}

# Deployment strategy - Recreate for single-replica stateful apps
strategy:
  type: Recreate

# Probes - using TCP socket as Jackett HTTP responses can be inconsistent
livenessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 5
  failureThreshold: 5
  tcpSocket:
    port: http

readinessProbe:
  enabled: true
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3
  tcpSocket:
    port: http
